# -- core --
Django==5.2.*
Pillow>=11,<13            # image processing for uploads (ImageField needs this)

# -- server and static files --
gunicorn>=23,<24
whitenoise>=6.8,<7        # serves static files without a separate nginx

# -- server database --
psycopg[binary]>=3.2,<4   # PostgreSQL; not needed on SQLite

# -- extra security (optional but recommended) --
argon2-cffi>=23,<26       # after installing, uncomment the Argon2 line in PASSWORD_HASHERS
