# ── هسته ──
Django==5.2.*
Pillow>=11,<13            # پردازش تصویرهای آپلودی (ImageField بدون این کار نمی‌کند)

# ── سرور و فایل‌های استاتیک ──
gunicorn>=23,<24
whitenoise>=6.8,<7        # سرو استاتیک بدون nginx جدا

# ── دیتابیس سرور ──
psycopg[binary]>=3.2,<4   # PostgreSQL؛ روی SQLite لازم نیست

# ── امنیت بیشتر (اختیاری ولی توصیه‌شده) ──
argon2-cffi>=23,<26       # بعد از نصب، خط Argon2 را در PASSWORD_HASHERS از کامنت دربیاور
