# بالا آوردن سایت یکتا

این فایل دقیقاً همان کارهایی است که باید انجام بدهی، به همان ترتیب.
هیچ قدمی را جا نینداز — مخصوصاً قدم ۲ (کلید مخفی) و قدم ۶ (ساختن مدیر).

---

## ۰) اول روی همین کامپیوتر تست کن

```bat
cd C:\projects\yekta-site

python -m pip install -r requirements.txt
python manage.py makemigrations
python manage.py migrate
python manage.py seed_site
python manage.py createsuperuser
python manage.py runserver
```

بعد `http://127.0.0.1:8000/` را باز کن.

**قبل از دیپلوی، یک‌بار با `DEBUG=False` هم تست کن** — چون سهل‌گیری‌های
حالت توسعه (از جمله خاموش‌بودن CSRF) آنجا خاموش می‌شوند و اگر جایی
مشکل داشته باشد، بهتر است همین‌جا معلوم شود:

```bat
set DJANGO_DEBUG=0
set DJANGO_SECRET_KEY=یک-کلید-تصادفی-بلند
set DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost
python manage.py collectstatic --noinput
python manage.py runserver
```

با `set DJANGO_DEBUG=1` به حالت عادی برگرد.

---

## ۱) حساب لیارا و ساخت برنامه

1. در [liara.ir](https://liara.ir) ثبت‌نام کن و کیف پول را شارژ کن.
2. `Liara CLI` را نصب کن: `npm i -g @liara/cli` و بعد `liara login`.
3. یک برنامه‌ی **Django** بساز؛ نامش را `yekta` بگذار
   (اگر نام دیگری گذاشتی، همان را در `liara.json` هم عوض کن).

---

## ۲) کلید مخفی — این قدم اجباری است

سایت با `DEBUG=False` و کلیدِ پیش‌فرض بالا نمی‌آید و عمداً خطا می‌دهد.
یک کلید واقعی بساز:

```bat
python -c "from django.core.management.utils import get_random_secret_key as g; print(g())"
```

خروجی را نگه دار؛ در قدم ۴ لازم می‌شود.

---

## ۳) دیتابیس PostgreSQL

در پنل لیارا یک دیتابیس **PostgreSQL** بساز و رشته‌ی اتصالش
(`postgresql://user:pass@host:port/dbname`) را بردار.

---

## ۴) متغیرهای محیطی

در پنل لیارا → برنامه‌ی `yekta` → بخش **متغیرها**، این‌ها را بگذار:

| نام | مقدار | توضیح |
|---|---|---|
| `DJANGO_DEBUG` | `0` | حتماً صفر |
| `DJANGO_SECRET_KEY` | کلید قدم ۲ | به هیچ‌کس نده |
| `DJANGO_ALLOWED_HOSTS` | `yekta.liara.run,yourdomain.ir` | دامنه‌هایت، با کاما |
| `DATABASE_URL` | رشته‌ی قدم ۳ | |
| `MEDIA_ROOT` | `/usr/src/app/media` | همان دیسکِ `liara.json` |
| `EMAIL_HOST` | `smtp.gmail.com` | نگاه کن به بخش «ایمیل» |
| `EMAIL_PORT` | `587` | |
| `EMAIL_HOST_USER` | جی‌میلت | |
| `EMAIL_HOST_PASSWORD` | «رمز عبور برنامه» | نه رمز اصلی جی‌میل |
| `DEFAULT_FROM_EMAIL` | `YEKTA <همان جی‌میل>` | |

متغیرهای پیامکی (`SMS_*`) را فعلاً خالی بگذار — بخش «پنل پیامکی» را ببین.

---

## ۵) دیسک رسانه — مهم

عکس‌های آپلودی روی فایل‌سیستمِ برنامه می‌نشینند و **با هر دیپلوی پاک
می‌شوند**، مگر روی یک دیسک دائمی باشند. در پنل لیارا یک **دیسک** به نام
`media` بساز. `liara.json` خودش آن را به مسیر درست وصل می‌کند.

> اگر این قدم را رد کنی، هر بار که سایت را به‌روز کنی همه‌ی عکس‌های یکتا
> از بین می‌روند. جدی‌اش بگیر.

---

## ۶) دیپلوی و راه‌اندازی اولیه

```bat
cd C:\projects\yekta-site
liara deploy
```

بعد از بالا آمدن، یک‌بار (و فقط یک‌بار) این‌ها را اجرا کن — از بخش
**کنسول/Shell** در پنل لیارا:

```bash
python manage.py migrate
python manage.py seed_site
python manage.py createsuperuser        # حساب خودت (رضا)
```

بعد وارد `https://yekta.liara.run/admin/` شو و در **حساب‌ها**:

- ردیف خودت → نقش = **مدیر سایت**.
- برای یکتا: از پنل استودیو → **تنظیمات** → **افزودن مدیر**، جی‌میل و
  شماره‌اش را بزن، یک رمز برایش تعیین کن و سطح دسترسی را
  **«یکتا — پنل استودیو»** بگذار. همان رمز را به خودش بده؛ از
  `https://yekta.liara.run/login/` با جی‌میل **یا** شماره تلفن وارد می‌شود.

---

## ۷) دامنه و SSL

در پنل لیارا → **دامنه‌ها** → دامنه‌ات را اضافه کن و رکورد DNS را طبق
راهنمای همان‌جا تنظیم کن. SSL را روشن کن (رایگان است). بعد دامنه را به
`DJANGO_ALLOWED_HOSTS` هم اضافه کن و دوباره دیپلوی کن.

---

## ۸) گزارش شبانه

`liara.json` یک cron روی `۲۰:۲۵ UTC` دارد که برابر **۲۳:۵۵ به وقت تهران**
است. همان است که هر شب گزارش می‌سازد. اگر خواستی دستی بسازی:

```bash
python manage.py nightly_report            # فقط امروز
python manage.py nightly_report --days 7   # هفت روز گذشته
```

---

# چیزهایی که باید بخری یا وصل کنی

سایت بدون هیچ‌کدام از این‌ها بالا می‌آید و کار می‌کند. هرکدام را که وصل
کردی، فقط متغیر محیطی‌اش را بگذار — **هیچ خطی از کد عوض نمی‌شود.**

## ۱. ایمیل (لازم — رایگان)

تایید ایمیل و بازیابی رمز از این می‌آید.

**جی‌میل، رایگان، ۵۰۰ ایمیل در روز** — برای این سایت خیلی بیشتر از کافی است:

1. در حساب گوگل، **تایید دو مرحله‌ای** را روشن کن.
2. برو به `myaccount.google.com/apppasswords` و یک **App Password** بساز.
3. همان ۱۶ نویسه را در `EMAIL_HOST_PASSWORD` بگذار (نه رمز اصلی).

> اگر روزی حجم بالا رفت، گزینه‌های ایرانی مثل «میل‌کیت» یا SMTP خود لیارا
> هست؛ ولی برای شروع لازم نیست.

## ۲. پنل پیامکی (اختیاری — بعداً)

کد آماده است و خاموش نشسته (`main/sms.py`). تا وقتی `SMS_PROVIDER` خالی
باشد، پیامک‌ها فقط در لاگ چاپ می‌شوند و **هیچ‌جای سایت نمی‌شکند**.

قیمت‌ها (شهریور ۱۴۰۴، تقریبی — پیش از خرید از خود سایتشان چک کن):

| سرویس | هزینه‌ی خط | هر پیامک (فارسی) | نکته |
|---|---|---|---|
| **کاوه‌نگار** | خط اشتراکی رایگان، خط اختصاصی از ~۲۵۰ هزار تومان | ~۱۲۰ تا ۱۹۰ تومان | مستندات بهتر، پرکاربردترین |
| **ملی‌پیامک** | خط از ~۲۰۰ هزار تومان | ~۱۱۰ تا ۱۸۰ تومان | ارزان‌تر، پنل شلوغ‌تر |
| **قاصدک** | خط از ~۳۰۰ هزار تومان | ~۱۳۰ تا ۲۰۰ تومان | خوب برای پیامک تاییدیه |

**برآورد واقعی برای این سایت:** اگر ماهی ۱۰۰ کارفرما ثبت‌نام کنند و هرکدام
۲ پیامک بگیرند، می‌شود ۲۰۰ پیامک ≈ **۳۰ تا ۴۰ هزار تومان در ماه**، به‌علاوه‌ی
یک‌بار هزینه‌ی خط.

**نظر من:** فعلاً نخر. با جی‌میل شروع کن؛ شماره‌ی تلفن همین حالا هم گرفته
و ذخیره می‌شود و ورود با شماره کار می‌کند. هر وقت ترافیک واقعی دیدی، خط
بخر و این سه متغیر را بگذار:

```
SMS_PROVIDER = kavenegar        # یا melipayamak یا ghasedak
SMS_API_KEY  = ...
SMS_SENDER   = ...              # شماره‌ی خط
```

## ۳. فضای ذخیره‌سازی ابری (اختیاری)

دیسکِ قدم ۵ برای شروع کافی است. اگر عکس‌ها از چند گیگ گذشت، **آبجکت
استوریج لیارا** (~۵ هزار تومان به‌ازای هر گیگ در ماه) بگیر و به من بگو تا
`STORAGES` را به آن وصل کنم.

## ۴. کش مشترک (اختیاری)

محدودیت نرخ درخواست الان روی حافظه‌ی هر ورکر است. اگر خواستی دقیق‌تر شود،
یک **Redis** روی لیارا بساز و به من بگو تا `CACHES` را وصل کنم.

---

# اندازه‌ی تصویرها

| کجا | اندازه | فرمت | نکته |
|---|---|---|---|
| **بنر بزرگ (تصویر بدون پس‌زمینه)** | **۱۶۰۰ × ۲۴۰۰ پیکسل** | **PNG با پس‌زمینه‌ی شفاف** | قدِ کامل، **پاها دقیقاً روی لبه‌ی پایین کادر**، بدون سایه (سایه را خود سایت می‌سازد). زیر ۲ مگابایت. |
| ویدیوی هیرو | ۱۹۲۰×۱۰۸۰ | MP4 (H.264) | بی‌صدا، ۱۰ تا ۲۰ ثانیه، زیر ۸ مگابایت |
| پوستر ویدیو | ۱۹۲۰×۱۰۸۰ | JPG | قاب اول همان ویدیو |
| عکس‌های گالری | عرض ۱۲۰۰ تا ۱۶۰۰ | JPG یا WEBP | ارتفاع آزاد — شبکه پینترستی است و نمی‌بُرد |
| تصویر سردر مجموعه | ۱۶۰۰×۲۰۰۰ | JPG | اگر خالی بگذاری، تازه‌ترین عکس همان مجموعه خودکار می‌نشیند |
| لوگوی برند | ۴۰۰×۴۰۰ | PNG شفاف | اگر نگذاری، حرف اول نام نشان داده می‌شود |
| تصویرهای چیدمان | عرض ۱۴۰۰ | JPG | |

سقف آپلود: تصویر ۸ مگابایت، ویدیو ۶۰ مگابایت.

---

# نگهداری روزمره

| کار | کجا |
|---|---|
| عکس تازه، حذف عکس | پنل استودیو ← عکس‌ها |
| مجموعه/نوع مدلینگ/محل/نوع همکاری | پنل استودیو ← فهرست‌ها |
| پاسخ به درخواست‌ها و گفتگو | پنل استودیو ← درخواست‌ها |
| کف بودجه و قواعد رزرو | پنل استودیو ← تنظیمات |
| افزودن مدیر | پنل استودیو ← تنظیمات (فقط مدیر سایت) |
| رنگ بنر، متن‌های هیرو، مشخصات | پنل جنگو ← پروفایل یکتا |
| تقویم (روزهای باز/بسته، تعطیلات) | پنل جنگو ← الگوی هفتگی / استثناها / قفل‌ها |
| گزارش‌ها و آمار | پنل استودیو ← گزارش‌ها |

---

# اگر چیزی خراب شد

| نشانه | علت معمول |
|---|---|
| `DisallowedHost` | دامنه در `DJANGO_ALLOWED_HOSTS` نیست |
| `RuntimeError` درباره‌ی کلید | `DJANGO_SECRET_KEY` ست نشده (قدم ۲) |
| عکس‌ها بعد از دیپلوی رفتند | دیسک `media` ساخته نشده (قدم ۵) |
| ایمیل نمی‌رسد | App Password اشتباه است، یا تایید دو مرحله‌ای روشن نیست |
| CSS به‌هم‌ریخته | `collectStatic` اجرا نشده — دوباره دیپلوی کن |
| «۴۲۹ کمی آرام‌تر» | محدودیت نرخ؛ چند دقیقه صبر کن (طبیعی است) |

لاگ‌ها: `liara logs --follow`
